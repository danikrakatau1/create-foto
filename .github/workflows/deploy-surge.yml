name: Deploy to Surge

on:
  push:
    branches:
      - main

workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Surge
        run: npm install --global surge

      - name: Deploy to Surge
        run: surge ./ create-foto.surge.sh --token ${{ secrets.SURGE_TOKEN }}
